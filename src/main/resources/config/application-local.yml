artemis:
  # added local config for windows based dev deployment
  # need to be changed before pushing to prod
  user-management:
    use-external: false
  version-control:
    url: http://localhost:8080
  continuous-integration:
    # Only necessary on Windows:
    docker-connection-uri: tcp://localhost:2375
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
